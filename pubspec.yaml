name: cli_router
description: >
  Router for CLIs inspired by shelf_router that uses spaces between segments instead of /
version: 0.0.3

repository: https://github.com/macss-dev/cli_router
homepage: https://github.com/macss-dev/cli_router
issue_tracker: https://github.com/macss-dev/cli_router/issues
documentation: https://pub.dev/documentation/cli_router/latest

environment:
  sdk: ^3.8.1

# dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

topics:
  - cli
  - router
  - macss